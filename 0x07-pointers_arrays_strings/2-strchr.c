#include "main.h"
/**
 * _strchr - a function that locates a character in a string
 * @s: the string
 * @c: the character housed in the string
 * Return: returns c if character is found else NULL
 */

char *_strchr(char *s, char c)
{
	char food[] = { }
	return (null); 
}
