#include "main.h"

/**
 * add - Adds two integers and returns the sum
 *
 * @k: number one.
 * @l: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int k, int l)
{
	return (k + l);
}
