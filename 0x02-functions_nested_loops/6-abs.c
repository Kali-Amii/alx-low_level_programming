#include "main.h"

/**
 * _abs - computes absolutes value of an integer
 *
 * @n: absolute value
 *
 * Return: n when n >= 0
 * -n when n < 0
 */

int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (-n);
}
